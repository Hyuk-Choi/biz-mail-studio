# Current portfolio/demo mode uses the local mock generator only.
MAIL_GENERATION_PROVIDER=mock

# Optional: set these later only when switching MAIL_GENERATION_PROVIDER=openai.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
